//! Ram disk block device driver. This is the simplest functional block device
//! implementation as it just reads and writes directory from memory instead of
//! any physical hardware,

pub struct RamDisk;
